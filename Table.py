
class Table:
	"""This holds all the essential information of those things on the table during the game.""" 
	def __init__(self)
		self.center = {}
		self.teamscores = [0, 0]
		self.allcards = tuple([c + s for s in ('d', 's', 'c', 'h') for c in ('a', 'k', 'q', 'j', 't', 'n')])
	
	def handDone(self):
		pass
		